experiment_name: base_experiment
seed: 7

problem:
  name: maxcut
  parameters:
    graph_family: cycle
    num_nodes: 4
    weight: 1.0

solver:
  name: brute_force
  parameters:
    max_variables: 20

output:
  directory: data/results
  tag: base
